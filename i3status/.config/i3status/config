general {
    output_format = "i3bar"
    separator = " | "
}

order += "cpu_temperature 0"
order += "memory"
order += "load"
order += "disk /"
order += "tztime local"

tztime local {
  format = "%A, %d %B %H:%M:%S"
}
