
# Set LD_LIBRARY_PATH based on hostname
if [[ $(hostname) == "hilliard-taa-lab" ]]; then
    source ~/.zshrc-hilliard
fi

if [[ $(hostname) == "gpucluster" ]]; then
    source ~/.zshrc-gpucluster
fi

# Local binaries
export PATH="$PATH:$HOME/.local/bin"

# zoxide
eval "$(zoxide init zsh)"
